<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cs.architecture">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.MvpActivity" />
        <activity android:name=".mvp.MvpActivity2" />
        <activity android:name=".mvvm.MvvmActivity" />
        <activity android:name=".jetpack.livedata.LiveDataActivity" />
        <activity android:name=".jetpack.databinding.BindingActivity" />
        <activity android:name=".jetpack.coroutines.CoroutinesActivity" />
        <activity android:name=".jetpack.navigation.NavigationActivity" />
    </application>

</manifest>